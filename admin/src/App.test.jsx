import React from 'react';

// Test components one by one to find the source of "U is not a constructor"
function TestApp() {
  return (
    <div>
      <h1>Test App Loading...</h1>
      <p>If you see this, basic React rendering works.</p>
    </div>
  );
}

export default TestApp;